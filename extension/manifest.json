{
  "manifest_version": 3,
  "name": "DNS Control",
  "version": "0.2.0",
  "description": "Pair with DNS Control: import dns-control-config.json from the dashboard, then use Chrome Secure DNS with your DoH URL. Blocking runs on the server.",
  "permissions": ["storage"],
  "host_permissions": ["<all_urls>", "https://*/*"],
  "background": {
    "service_worker": "background.js"
  },
  "options_page": "options.html"
}
